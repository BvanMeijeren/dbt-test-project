
  
    
    

    create  table
      "dev"."main"."my_first_dbt_model__dbt_tmp"
  
    as (
      --

with source_data as (

    select 1 as id
    union all

)

select *
from spotify_tracks
    );
  
  